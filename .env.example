# fin-agent environment configuration
# Copy to .env and adjust as needed.

# ==== AUTHENTICATION ====
# Password for HTTP Basic Auth and WebSocket connections
# Required for production; leave empty for open local dev access
AUTH_PASSWORD=

# ==== ANTHROPIC API ====
# One of these is required for the agent to work:
ANTHROPIC_API_KEY=
# or use Claude Max/Pro subscription OAuth token:
# CLAUDE_CODE_OAUTH_TOKEN=

# ==== DATABASE ====
# SQLite database path (defaults to ~/.finagent/data.db when unset)
FINAGENT_DATABASE_PATH=~/.finagent/data.db

# ==== CORS ====
# Comma-separated list of allowed origins
# Leave empty to allow all origins (default for local dev)
ALLOWED_ORIGINS=

# ==== SERVER ====
# Port to run on (default: 3000)
PORT=3000

# ==== PLAID INTEGRATION (optional) ====
# PLAID_CLIENT_ID=
# PLAID_SECRET=
# PLAID_ENV=sandbox

# ==== PRODUCTION (Render.com) ====
# These are set automatically by render.yaml when deployed:
# FINAGENT_DATABASE_PATH=/var/data/data.db
# NODE_ENV=production
