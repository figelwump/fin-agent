# Optional overrides for fin-agent CLI tools
# Copy to .env and adjust as needed. All values are optional.

# SQLite database path (defaults to ~/.finagent/data.db when unset)
FINAGENT_DATABASE_PATH=~/.finagent/data.db
