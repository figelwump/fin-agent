# Claude API Configuration
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-api-key-here

# OpenAI API Key (if using OpenAI models as fallback)
OPENAI_API_KEY=your-openai-api-key-here

# Database Configuration
# Path to the SQLite database created by fin-cli
# Default: ~/.findata/transactions.db
SQLITE_DATABASE_PATH=~/.findata/transactions.db

# Server Configuration
PORT=3000

# Agent Configuration
# Working directory for the agent (relative to project root)
AGENT_CWD=agent

# Model selection (opus, sonnet, haiku)
MODEL=opus